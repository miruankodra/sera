/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AnimateOnClickDirective } from './animateOnClick.directive';

describe('Directive: AnimateOnClick', () => {
  it('should create an instance', () => {
    const directive = new AnimateOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
