Phase 1 — Foundation
[x] Task 1: Fresh Laravel install + packages

"Inside the api/ folder, create a fresh Laravel 11 project. Install all packages listed in CLAUDE.md under Required Packages, run the publish commands, and configure the .env file with the correct drivers (redis queue, redis cache, reverb broadcast, predis client). Do not create any application code yet."

[x] Task 2: Docker Compose setup

"Create the docker-compose.yml at the repo root with all 6 services defined in CLAUDE.md (app, nginx, mysql, redis, queue, reverb). Create the custom PHP 8.3-FPM Dockerfile for the app image. Create the nginx config. Make sure docker compose up -d starts cleanly and the Laravel welcome page is reachable."


Phase 2 — Database
[x] Task 3: Migrations

"Create all migrations for the schema defined in CLAUDE.md. Use the exact column names, types, nullable flags, indexes, and foreign key constraints shown. Respect the order: users → greenhouses → sensors → sensor_readings → devices → device_commands → automation_rules → alerts → tasks. Run php artisan migrate and confirm all tables are created with no errors."

[x] Task 4: Models, relationships, factories, seeders

"Create Eloquent models for all 9 tables with correct relationships, $fillable arrays, casts (including tasks.payload as array, recorded_at as datetime), and soft deletes on greenhouses and automation_rules. Create factories with realistic faker data for each model. Create a main DatabaseSeeder that seeds 2 users, 3 greenhouses each, sensors, 30 days of sensor readings with a realistic diurnal pattern, devices, automation rules, alerts, and tasks. Run php artisan migrate:fresh --seed and confirm it completes without errors."


Phase 3 — Authentication
[x] Task 5: Auth endpoints

"Implement POST /api/v1/auth/register, POST /api/v1/auth/login, and POST /api/v1/auth/logout using Laravel Sanctum. Use FormRequests for validation. Return the token on login. Protect logout with auth:sanctum. Write Pest feature tests covering: successful register, successful login, invalid credentials (422), logout invalidates token. All tests must pass."


Phase 4 — Core API
[x] Task 6: Greenhouses CRUD

"Implement all greenhouse endpoints from CLAUDE.md. Create GreenhouseController, GreenhousePolicy (user can only access their own greenhouses), StoreGreenhouseRequest, UpdateGreenhouseRequest, and GreenhouseResource. Soft deletes must work. Write Pest feature tests covering: CRUD happy paths, 401 unauthenticated, 403 cross-user access. All tests must pass."

[x] Task 7: Sensors + readings

"Implement all sensor and sensor_reading endpoints. The POST /sensors/{id}/readings ingestion endpoint must fire a SensorReadingCreated event after saving (do not evaluate automation rules here — that happens in the queue). Add a composite index on (sensor_id, recorded_at). The GET readings endpoint must support ?from=&to= date range filtering. Write Pest tests. All tests must pass."

[x] Task 8: Devices + commands

"Implement all device endpoints plus POST /devices/{id}/command. The command endpoint must update devices.status and devices.last_commanded_at, create a row in device_commands with source = manual, and broadcast a DeviceStatusChanged event to the private greenhouse channel. Write Pest tests. All tests must pass."

[x] Task 9: Automation rules

"Implement automation rule endpoints. Create the AutomationEngine service class that accepts a SensorReading and evaluates all active rules for that greenhouse. Create the EvaluateAutomationRules job that calls AutomationEngine. Create the SensorReadingCreated event listener that dispatches this job to the Redis queue. When a rule triggers: create a device_command with source = automation, update device status, create an alert, and broadcast AutomationRuleTriggered and AlertFired events. Write Pest unit tests for AutomationEngine covering all operators (gt, lt, eq, gte, lte) and edge cases (no active rules, disabled device). All tests must pass."

[x] Task 10: Alerts + tasks

"Implement alert endpoints (GET /greenhouses/{id}/alerts, POST /alerts/{id}/read returns 204). Implement task endpoints (full CRUD). tasks.payload must be stored as JSON and cast to array on the model. Write Pest tests. All tests must pass."


Phase 5 — Real-time
[x] Task 11: Broadcasting

"Configure Laravel Reverb. Define all 4 broadcast events from CLAUDE.md with their exact payloads. Define the GreenhouseChannel private channel in routes/channels.php — authorise only if the authenticated user owns the greenhouse. Confirm php artisan reverb:start runs without errors."


Phase 6 — Quality
[x] Task 12: Security hardening

"Add throttle middleware: 5 attempts per minute on auth endpoints, 60 per minute on the sensor ingestion endpoint. Verify all controllers call $this->authorize() before any DB operation. Confirm no raw SQL anywhere — all queries use Eloquent or query builder with parameter binding. Add CORS configuration to allow only the Angular app origin."

[x] Task 13: API documentation

"Add l5-swagger OpenAPI annotations to all controllers and resource classes. Run php artisan l5-swagger:generate and confirm the Swagger UI is accessible at /api/documentation with all endpoints documented."

[x] Task 14: CI pipeline

"Create .github/workflows/ci.yml that runs on every push: sets up PHP 8.3, installs composer dependencies with cache, starts a MySQL 8 service container, runs migrations, executes the full Pest suite, and runs Laravel Pint. The pipeline must pass cleanly."


───────────────────────────────────────────────────────────
Phase 7 — Frontend API Integration (Angular)
───────────────────────────────────────────────────────────

[x] F1: Authentication layer

"Fix the login flow end-to-end: update API_URL in environment.ts to point at the real backend, replace mock HttpPaths with real auth endpoints (auth/login, auth/logout, auth/register), fix HttpService generics to separate request and response types and accept any 2xx status code. Update LoginDto to match the real API response shape { data: { token, user } }. Create AuthService with login(), logout(), and isAuthenticated() methods. Create a functional AuthGuard (CanActivateFn) that checks for a stored Sanctum token and redirects to /login if missing. Apply the guard to the tabs route. Update LoginComponent to use AuthService with a loading signal and Albanian error toast. Add a disabled input to SeButtonComponent. Wire logout in ProfileComponent."

[ ] F2: Home page — real greenhouses

"Replace all mock data in HomeComponent with real API calls. Call GET /api/v1/greenhouses to list the user's greenhouses. For each greenhouse, display a summary card with the latest sensor readings (GET /sensors/{id}/readings, last 1). Add a create greenhouse flow via POST /api/v1/greenhouses. Add delete via DELETE /api/v1/greenhouses/{id}. Add GreenhouseService to centralise all greenhouse API calls."

[ ] F3: Greenhouse detail — sensors, devices, automation rules

"Replace all mock data in GreenhouseComponent with real API calls. Load sensors via GET /greenhouses/{id}/sensors and latest reading per sensor via GET /sensors/{id}/readings. Load devices via GET /greenhouses/{id}/devices. Wire the device toggle to POST /devices/{id}/command (turn_on / turn_off). Load automation rules via GET /greenhouses/{id}/automation-rules. Support create/edit/delete of rules via POST/PUT/DELETE endpoints. Add SensorService and DeviceService."

[ ] F4: WebSocket — real-time updates

"Wire WebSocketService into GreenhouseComponent and HomeComponent. Subscribe to the private-greenhouse.{id} channel on entering a greenhouse view and unsubscribe on leave. Handle NewSensorReading → update sensor card value live. Handle DeviceStatusChanged → update device toggle state. Handle AlertFired → show a toast notification. Handle AutomationRuleTriggered → show an activity indicator."

[ ] F5: Profile page — real user data

"Replace hardcoded profile data with real API calls. Load user via GET /api/v1/user on page init. Wire an edit form to PUT /api/v1/user (name, email, timezone, locale, notification_preferences). Wire a password form to PUT /api/v1/user/password (current_password + new_password + confirmation). Implement avatar upload via POST /api/v1/user/avatar (multipart/form-data) and delete via DELETE /api/v1/user/avatar. Add UserService."

[ ] F6: Alerts

"Display real alerts for the active greenhouse. Call GET /greenhouses/{id}/alerts on load. Implement mark-as-read via POST /alerts/{id}/read (204). Show an unread alert badge/count in the greenhouse or home view. Real-time new alerts arrive via the WebSocket AlertFired event wired in F4. Add AlertService."

[ ] F7: Calendar / tasks — backend sync

"Sync CalendarComponent tasks with the backend instead of storing them only in Capacitor Preferences. On page init, load tasks from GET /greenhouses/{id}/tasks and merge with the local cache. On create, call POST /greenhouses/{id}/tasks in addition to saving locally. On complete, call PUT /tasks/{id} to sync is_completed. On delete, call DELETE /tasks/{id}. Resolve conflicts by treating the backend as the source of truth."

[ ] F8: Reports page

"Implement the Reports page (currently a stub). Fetch sensor readings over a date range via GET /sensors/{id}/readings?from=&to= and render trend charts. Show KPI summary cards (latest value per sensor type). Show a threshold breach log from GET /greenhouses/{id}/alerts filtered by date. Show automation activity derived from device commands. Show task completion stats from GET /greenhouses/{id}/tasks."
