# Dev Tasks

## Calendar Feature

- [ ] Build API endpoints for greenhouse tasks (CRUD: create, read, update, delete)
- [ ] Integrate API endpoints in `TaskService` — replace local-only storage with API calls, keep Capacitor Preferences as offline cache
- [ ] Replace dummy greenhouse/device data in `TaskService` with real API call
